basePath: /api
definitions:
  api.ErrorResponse:
    properties:
      error:
        type: string
    type: object
  api.Hole:
    properties:
      custom:
        type: boolean
      hole:
        type: integer
      top_strokes:
        description: |-
          TopStrokes is the lowest recorded stroke count for this hole, or null
          if it has no recorded scores.
        type: integer
    type: object
  api.ScoreRecord:
    properties:
      channel_id:
        type: string
      guild_id:
        type: string
      hole:
        type: integer
      message_id:
        type: string
      recorded_at:
        type: string
      share_link:
        type: string
      strokes:
        type: integer
      user_id:
        type: string
      username:
        type: string
    type: object
info:
  contact: {}
  description: |-
    Read-only REST API for querying putt.day scores tracked by the albatross Discord bot.
    Recording and removing scores is only done via the Discord bot itself; this API has no
    authentication, so it intentionally exposes no write or delete endpoints.
  license:
    name: MIT
    url: https://github.com/alexbathome/albatross/blob/main/LICENSE
  title: Albatross API
  version: "1.0"
paths:
  /holes:
    get:
      description: Returns up to limit registered holes, descending by hole number.
      parameters:
      - description: Max results (default 20, max 1000)
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/api.Hole'
            type: array
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/api.ErrorResponse'
      summary: List recent holes
      tags:
      - holes
  /holes/{hole}/top:
    get:
      description: Returns up to limit scores for hole, ascending by strokes (lowest
        first).
      parameters:
      - description: Hole number
        in: path
        name: hole
        required: true
        type: integer
      - description: Max results (default 10, max 25)
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/api.ScoreRecord'
            type: array
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/api.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/api.ErrorResponse'
      summary: Leaderboard for a hole
      tags:
      - scores
  /scores:
    get:
      description: Returns each matching user's best (lowest-stroke) play per hole,
        holes descending. Matches case-insensitively against the username recorded
        with each score.
      parameters:
      - description: Username to search for (substring match)
        in: query
        name: username
        required: true
        type: string
      - description: Max results (default 50, max 100)
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/api.ScoreRecord'
            type: array
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/api.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/api.ErrorResponse'
      summary: Search scores by username
      tags:
      - scores
  /users/{userID}/holes/{hole}:
    get:
      description: 'Returns up to limit of userID''s records for hole, ascending by
        strokes. Not deduplicated: every recorded play is returned.'
      parameters:
      - description: Discord user ID
        in: path
        name: userID
        required: true
        type: string
      - description: Hole number
        in: path
        name: hole
        required: true
        type: integer
      - description: Max results (max 25)
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/api.ScoreRecord'
            type: array
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/api.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/api.ErrorResponse'
      summary: A user's scores for a hole
      tags:
      - scores
swagger: "2.0"
